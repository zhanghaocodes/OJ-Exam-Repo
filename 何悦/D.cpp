#include<cstdio>
#include<algorithm>
#include<cmath>
#include <iostream>
#include <vector>
#include <stack>
using namespace std;
int num[9][9];
int main() {
	while(check) {

		// 输入原数独的值
		for(int i=0; i<9; i++) {
			//改变对角线上的值（1-9随机数）
		}
		//改变第一列和最后一列的值
		//改变第一排和最后一排的值 
	}
}
bool check() {
	//检查数组是否满足条件 
	for(int i=0; i<9; i++) {
		checkpoint(i);
	}
}
int checkpotint(int i;) {
	//检查该对角线位置所属行列是否满足条件
	//检查所有九宫格的值是否满足条件 
}
int rowsum(int i) {
	//计算每行的值 是否 等于45
}

int colsum(int j) {
	//计算每列的值 是否 等于45
}
int gridsum(int j) {
	//计算该位置所属9宫格的和 是否 等于45
}

